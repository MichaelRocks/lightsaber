task wrapper(type: Wrapper) {
    gradleVersion = '2.3'
}

allprojects {
    buildscript {
        repositories {
            mavenCentral()
        }
    }

    repositories {
        mavenCentral()
    }
}

ext {
    shadowVersion = '1.2.1'

    javaxInjectVersion = '1'
    jcommanderVersion = '1.47'
    asmVersion = '5.0.3'
    commonsIoVersion = '2.4'

    junitVersion = '4.12'
}
