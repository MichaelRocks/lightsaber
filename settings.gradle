rootProject.name = 'lightsaber'

include ':core'
include ':processor'

if (!properties['skipSamples'].toBoolean()) {
    include ':samples:sample-java'
    include ':samples:sample-kotlin'
    include ':samples:sample-android-kotlin'
}
