buildscript {
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "com.michaelrocks:lightsaber-gradle-plugin:$version"
    }
}

plugins {
    id 'com.github.johnrengelman.shadow' version '1.2.1'
}

apply plugin: 'java'
apply plugin: 'kotlin'
apply plugin: 'application'
apply plugin: 'com.michaelrocks.lightsaber'

sourceCompatibility = 1.6

mainClassName = 'com.michaelrocks.lightsaber.sample.LightsaberSample'

dependencies {
    compile "com.michaelrocks:lightsaber-core:$version"
    compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlinVersion"

    testCompile "junit:junit:$junitVersion"
}

jar {
    destinationDir = file('build/jar')

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
